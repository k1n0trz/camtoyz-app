{
  "name": "@camtoyz/room-server",
  "version": "0.1.0",
  "private": true,
  "description": "Servidor efímero de membresía y señalización para salas Camtoyz",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server/src/index.js",
    "test": "npm run build && node --test --test-concurrency=1 dist/server/test/roomServer.test.js"
  },
  "dependencies": {
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "socket.io-client": "^4.8.3",
    "typescript": "^5.3.3"
  }
}
